import 'package:flutter/material.dart';

/// Design tokens — mirrors Section B1 of the Phase 1 design system.
class AppColors {
  static const primary = Color(0xFF2563EB);
  static const primaryDark = Color(0xFF1D4ED8);
  static const primaryTint = Color(0xFFDBEAFE);

  static const secondary = Color(0xFFF59E0B); // CTA / conversion only
  static const secondaryDark = Color(0xFFD97706);

  static const background = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceAlt = Color(0xFFF1F5F9);

  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);
  static const border = Color(0xFFE2E8F0);

  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFD97706);
  static const error = Color(0xFFDC2626);
  static const info = Color(0xFF0EA5E9);
  static const star = Color(0xFFFBBF24);
}
